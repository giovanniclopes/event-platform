query GetLessonBySlug($slug: String) {
    lesson(where: {slug: $slug}) {
        title
        videoId
        description
        complementaryMaterial {
            id
        }
        teacher {
            name
            bio
            avatarURL
        }
    }
}